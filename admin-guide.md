# 邻选 · 团长操作手册

超级管理员拥有全部权限；普通团长只能编辑自己创建的轮次。

---

## 一、首次部署（一次性操作）

1. 注册 [Supabase](https://supabase.com) 免费账号，创建项目
2. 在 Supabase → SQL Editor 执行 `supabase-schema.sql`
3. 复制 Supabase 的 Project URL 和 Anon Key
4. 在 GitHub 创建仓库，上传所有文件，Settings → Pages 启用 GitHub Pages
5. 打开部署网址，填入 URL 和 Key
6. 网址后加 `#admin`，用默认超管账号 `admin` / `admin123` 登录

---

## 二、登录后台

浏览器打开 `你的网址#admin`，输入用户名和密码登录。团长可在登录页点击「注册团长」自助注册。

---

## 三、创建与管理轮次

**轮次设置 → 创建新轮次**

- 填写名称、截止时间
- 创建轮次后自动归属当前团长，超级管理员可在轮次列表中重新分配
- 创建后为**策划中**，添加商品后点**开团**变为进行中
- 四种状态：策划中 → 进行中 → 已截止 / 已停止
- 允许多个轮次同时进行，商品和订单完全隔离

**批量操作**：一键停止 / 启用 / 多选删除

---

## 四、添加商品

**商品管理 → 选择轮次 → 添加商品**

- 名称、图片（点击放大）、规格与价格
- **整装拆分**：输入整装数量/总价/每份大小，一键生成
- **标签排序**：可自定义顺序，顾客端同步
- 库存：留空不限，卖完自动已售罄

---

## 五、称重商品

适用于只有单价无确定总价的称重类商品。

1. 勾选「称重商品」→ 填单价（¥/kg 或 ¥/份）+ 预估克重
2. 保存后自动生成 `1份`，可手动补充 `1/2份` 等
3. 顾客下单后状态为「待称重确认」
4. 到货后**订单汇总 → 称重分配**，录入实际重量
5. **单价 × 实际重量 × 份额 = 最终价格**

---

## 六、订单处理

**订单汇总**：按人按轮次分组，同商品合并，显示下单时间和拼单状态。

**砍单**：点击「砍单」→ 输入每项取消数量（可部分取消）→ 填写原因 → 确认。

**采购清单**：按商品汇总，支持复制。支持导出 Excel。

---

## 七、收款

**收款清单**：设置运费/包装费，自动均摊到分。称重确认后会计入。

---

## 八、拼单组队

顾客端可发起/加入拼单队伍，满员自动下单。

- 商品卡片展开「组队」区域，同一商品多队伍共存
- 发起人设目标份数和分成份数，其他人加入选份额
- 满员后系统自动为全队生成订单
- 团长订单汇总可监控队伍进度和成员信息

---

## 九、团长账号管理（超级管理员）

- **「团长管理」tab**：添加/删除团长、修改密码、查看明文密码
- **轮次设置**：每个轮次旁有下拉框可重新指定归属团长
- **普通团长**：可在登录页点击「注册团长」自助注册

---

## 十、常见操作

### 编辑商品
商品管理 → 点击商品右侧「编辑」→ 修改后点「保存修改」。

### 上下架/删除商品
商品管理 → 点击「下架」隐藏商品，「删除」从所有订单中标记移除。

### 导入导出商品
商品管理 →「导出商品」下载 Excel，「导入商品」批量恢复。

### 生成二维码
进入「下单二维码」tab，保存图片发到业主微信群。

### 修改站点标题
轮次设置 → 顾客端标题 → 输入新值 → 保存。仅超级管理员可操作。

### 修改密码
超级管理员在「团长管理」tab 中修改任意团长的密码。

### 查看顾客反馈
轮次设置 →「顾客反馈」→「查看反馈」，可标记已读。

---

## 十一、忘记密码

联系超级管理员在后台「团长管理」中重置。

超级管理员本人忘记密码，在 Supabase SQL Editor：
```sql
SELECT * FROM leaders;
```

---

## 十二、小贴士

- 售罄商品会自动沉到列表底部，显示「已售罄」标记
- 库存每 10 秒自动刷新，多人同时下单不会超卖（数据库行锁保护）
- 顾客下单后在页面顶部「查/改订单」可自行查询和取消
- 轮次切换时顾客购物车自动清空
- 允许多个轮次同时进行中，不同品类分开团购
- 商品可导出 Excel 再导入到新轮次
